import React from "react"

const EditComponent = props => {

    const {product_id} = props

    return(
        <div>
            <h1>Edit Component {product_id}</h1>
        </div>
    )
}
export default EditComponent
